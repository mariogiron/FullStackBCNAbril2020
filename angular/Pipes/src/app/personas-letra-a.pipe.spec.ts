import { PersonasLetraAPipe } from './personas-letra-a.pipe';

describe('PersonasLetraAPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonasLetraAPipe();
    expect(pipe).toBeTruthy();
  });
});
