import { PersonasLetraPipe } from './personas-letra.pipe';

describe('PersonasLetraPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonasLetraPipe();
    expect(pipe).toBeTruthy();
  });
});
